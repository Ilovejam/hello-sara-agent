OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4.1-mini
# User chooses wait in chat ("3 minutes", "1 hour").
# These only bound / demo-clamp parsing — not a fixed assignment delay.
MIN_DELAY_SECONDS=5
MAX_DELAY_SECONDS=86400

# Vercel only — local uses ./data
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
